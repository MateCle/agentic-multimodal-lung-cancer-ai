# Core dependencies (versioned for reproducibility, flexible for patch updates)
numpy~=2.4
pandas~=2.3
scikit-learn~=1.8
scikit-survival~=0.27
xgboost~=3.2
lifelines~=0.30
matplotlib~=3.10
shap~=0.48
joblib~=1.5
langgraph~=1.1
openai~=2.0

# Test dependencies
pytest~=9.0

# Optional provider (enable only if you use Anthropic in src/orchestrator/llm.py)
# anthropic~=0.50

# Optional retrieval acceleration for src/evaluation/benchmark_faiss.py
# faiss-cpu~=1.8
